# Agents schedule layout

Replaced the crowded schedule columns with a quiet list using Manifest spacing, typography, surfaces, and disclosure conventions. Agent and job names wrap in full; schedule, result, and actions remain visible. Recent runs, model/budget, and raw configuration stay available under Details. Routing diagnostics are collapsed. Schedule groups use keyboard-operable buttons with expanded state.

Validated desktop, intermediate, and phone widths in Jarvis and light themes against live read-only data with locally overridden assets. No horizontal overflow or browser errors; Details opens correctly. Focused Agents, Spirits, and cockpit CSS tests and JavaScript syntax checks pass. No schedules or live jobs were mutated during verification.
