{
  "name": "@aion/portal-mcp",
  "version": "0.1.0",
  "description": "MCP tools for the AION team portal API",
  "type": "module",
  "bin": {
    "aion-portal-mcp": "dist/index.js"
  },
  "files": [
    "dist/client.d.ts",
    "dist/client.js",
    "dist/client.js.map",
    "dist/index.d.ts",
    "dist/index.js",
    "dist/index.js.map",
    "dist/server.d.ts",
    "dist/server.js",
    "dist/server.js.map",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "prepack": "npm run build",
    "test": "npm run build && node --test dist/*.test.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
